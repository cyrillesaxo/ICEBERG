# Indexing notes

The project landing page is prepared for `https://cyrillesaxo.github.io/ICEBERG/` and the repository includes a GitHub Pages deployment workflow.

After this branch is merged, GitHub Pages still needs to be configured for this repository to use **GitHub Actions** as its publishing source if it is not already enabled. Once the page is live, validate the public URL, structured data, sitemap, and crawler access before submitting it to search tooling.

Do not interpret indexing or AI-answer visibility as guaranteed. The repository should continue to earn discoverability through original releases, benchmark evidence, case studies, external adoption, citations, and links rather than thin SEO pages.
