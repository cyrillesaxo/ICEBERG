// This fixture intentionally covers only a small subset of checkout behavior.
// UI Iceberg should therefore surface important unverified journey scenarios.

test("checkout success shows confirmation", async () => {
  // success complete confirmation payment
});

test("declined payment can retry", async () => {
  // declined payment retry card error
});

test("validation error can be corrected", async () => {
  // validation invalid required error
});
